- name: Configure servers
  hosts: servers
  become: true

  vars:
    users:
      - name: student
        ssh_key: "{{ lookup('file', lookup('env', 'HOME') + '/.ssh/id_ed25519.pub') }}"

  roles:
    - user
    - ssh