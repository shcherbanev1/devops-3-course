- name: Prepare
  hosts: all
  become: true

  tasks:
    - name: Install OpenSSH server
      ansible.builtin.apt:
        name: openssh-server
        state: present
        update_cache: true

    - name: Start SSH service
      ansible.builtin.service:
        name: ssh
        state: started